teks = open("catatan.txt", encoding="utf8").read().split()
def bubblesort(arr):
    for i in range(len(arr)-1,0,-1):
        for j in range(0,len(arr)-1):
            if arr[j] > arr[j+1]:
                arr[j], arr[j+1] = arr[j+1], arr[j]
    print(arr)
bubblesort(teks)
